import{f as n,g as e,J as s}from"./common-03e46d7f.js";import{_ as a}from"./common-af9dd5a8.js";const t='{"title":"起飞了！GitLab CI + Docker 项目部署实战","frontmatter":{"date":"2024-04-07","title":"起飞了！GitLab CI + Docker 项目部署实战","tags":["git"],"describe":null},"headers":[{"level":2,"title":"项目 CI/CD 配置","slug":"项目-ci-cd-配置"},{"level":3,"title":"阶段","slug":"阶段"},{"level":3,"title":"作业","slug":"作业"},{"level":3,"title":"流水线执行","slug":"流水线执行"},{"level":3,"title":"Gitlab Runner","slug":"gitlab-runner"},{"level":3,"title":"再次测试流水线","slug":"再次测试流水线"},{"level":3,"title":"遇到的问题","slug":"遇到的问题"},{"level":2,"title":"Docker","slug":"docker"},{"level":3,"title":"重要概念","slug":"重要概念"},{"level":3,"title":"如何生成 Docker 镜像","slug":"如何生成-docker-镜像"},{"level":2,"title":".gitlab-ci.yml 文件编写","slug":"gitlab-ci-yml-文件编写"}],"relativePath":"docs/git/gitlab-ci-start.md","lastUpdated":1779259473389.7424}';var o={};const c=[s('<p>之前有一篇<a href="https://juejin.cn/post/7349561234931515433" target="_blank" rel="noopener noreferrer">队友升职，被迫解锁 Jenkins（所以，前端需要学习 Jenkins 吗？🤔）</a>如何使用 Jenkins 来部署项目，看评论有好几个人都说怎么不用 GitLab CI，刚好有一个新的 Node 项目完成需要部署，尝试用 GitLab CI + docker 的方式去部署，写了这篇文章，希望对大家有帮助。</p><h2 id="项目-ci-cd-配置"><a class="header-anchor" href="#项目-ci-cd-配置" aria-hidden="true">#</a> 项目 CI/CD 配置</h2><p>首先点击 CI/CD，创建一个新的 GitLab CI</p><p><img src="/_assets/gitlab-ci-create.7de798d0.png" alt="gitlab-ci-create"></p><p>GitLab CI 是通过名为 <code>.gitlab-ci.yml</code> 的文件进行配置，该文件位于仓库的根目录下。</p><p><img src="/_assets/gitlab-ci-create-more.24981d81.png" alt="gitlab-ci-create-more"></p><p>新建的 GitLab CI 配置文件如下：</p><div class="language-yml"><pre><code><span class="token comment"># 这是一个示例的 GitLab CI/CD 配置文件，应该可以直接运行而无需任何修改。</span>\n<span class="token comment"># 它展示了一个基本的 3 个阶段的 CI/CD 流水线。在这里，我们使用 echo 命令来模拟流水线的执行，而不是真正的测试或脚本。</span>\n<span class="token comment">#</span>\n<span class="token comment"># 流水线由独立的作业组成，这些作业运行脚本，并被分组到阶段中。</span>\n<span class="token comment"># 阶段按顺序运行，但阶段内的作业并行运行。</span>\n<span class="token comment">#</span>\n<span class="token comment"># 欲了解更多信息，请参阅：https://docs.gitlab.com/ee/ci/yaml/index.html#stages</span>\n<span class="token comment">#</span>\n<span class="token comment"># 你可以将这个模板复制粘贴到一个新的 `.gitlab-ci.yml` 文件中。</span>\n<span class="token comment"># 请不要使用 `include:` 关键字将此模板添加到现有的 `.gitlab-ci.yml` 文件中。</span>\n<span class="token comment">#</span>\n<span class="token comment"># 要贡献 CI/CD 模板的改进，请遵循开发指南：</span>\n<span class="token comment"># https://docs.gitlab.com/ee/development/cicd/templates.html</span>\n<span class="token comment"># 此特定模板位于：</span>\n<span class="token comment"># https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Getting-Started.gitlab-ci.yml</span>\n\n<span class="token key atrule">stages</span><span class="token punctuation">:</span> <span class="token comment"># 作业的阶段列表，以及它们的执行顺序</span>\n  <span class="token punctuation">-</span> build\n  <span class="token punctuation">-</span> test\n  <span class="token punctuation">-</span> deploy\n\n<span class="token key atrule">build-job</span><span class="token punctuation">:</span> <span class="token comment"># 此作业运行在构建阶段，该阶段首先运行。</span>\n  <span class="token key atrule">stage</span><span class="token punctuation">:</span> build\n  <span class="token key atrule">script</span><span class="token punctuation">:</span>\n    <span class="token punctuation">-</span> echo &quot;Compiling the code<span class="token punctuation">...</span>&quot;\n    <span class="token punctuation">-</span> echo &quot;Compile complete.&quot;\n\n<span class="token key atrule">unit-test-job</span><span class="token punctuation">:</span> <span class="token comment"># 此作业运行在测试阶段。</span>\n  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test <span class="token comment"># 仅当构建阶段的作业成功完成时，它才会开始运行。</span>\n  <span class="token key atrule">script</span><span class="token punctuation">:</span>\n    <span class="token punctuation">-</span> echo &quot;Running unit tests<span class="token punctuation">...</span> This will take about 60 seconds.&quot;\n    <span class="token punctuation">-</span> sleep 60\n    <span class="token punctuation">-</span> echo &quot;Code coverage is 90%&quot;\n\n<span class="token key atrule">lint-test-job</span><span class="token punctuation">:</span> <span class="token comment"># 此作业也在测试阶段运行。</span>\n  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test <span class="token comment"># 它可以与 unit-test-job 同时运行（并行）。</span>\n  <span class="token key atrule">script</span><span class="token punctuation">:</span>\n    <span class="token punctuation">-</span> echo &quot;Linting code<span class="token punctuation">...</span> This will take about 10 seconds.&quot;\n    <span class="token punctuation">-</span> sleep 10\n    <span class="token punctuation">-</span> echo &quot;No lint issues found.&quot;\n\n<span class="token key atrule">deploy-job</span><span class="token punctuation">:</span> <span class="token comment"># 此作业在部署阶段运行。</span>\n  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy <span class="token comment"># 仅当测试阶段中的 *所有* 作业成功完成时，它才会运行。</span>\n  <span class="token key atrule">script</span><span class="token punctuation">:</span>\n    <span class="token punctuation">-</span> echo &quot;Deploying application<span class="token punctuation">...</span>&quot;\n    <span class="token punctuation">-</span> echo &quot;Application successfully deployed.&quot;\n</code></pre></div><h3 id="阶段"><a class="header-anchor" href="#阶段" aria-hidden="true">#</a> 阶段</h3><p>脚本中使用 <code>stages</code> 字段定义了三个阶段：build、test 和 deploy，按照由上到下的顺序执行，和 Jenkins 中的 stages 一样。</p><h3 id="作业"><a class="header-anchor" href="#作业" aria-hidden="true">#</a> 作业</h3><p>脚本包含四个作业：build-job、unit-test-job、lint-test-job 和 deploy-job，作业中通过 <code>stage</code> 字段来定义当前作业属于哪个阶段，类似于 Jenkins 中的 steps。</p><p>其中 unit-test-job、lint-test-job 属于同一个阶段 test。</p><p>作业中通过 <code>script</code> 定义执行脚本。</p><h3 id="流水线执行"><a class="header-anchor" href="#流水线执行" aria-hidden="true">#</a> 流水线执行</h3><p>提交 <code>.gitlab-ci.yml</code> 的修改即可触发 CI 流程，此时还未启动任何 Runner，所有的作业都会被标记为等待状态。</p><p><img src="/_assets/gitlab-ci-runner-not.5e4de8f9.png" alt="gitlab-ci-runner-not"></p><p>那么 Gitlab Runner 又是什么呢？</p><h3 id="gitlab-runner"><a class="header-anchor" href="#gitlab-runner" aria-hidden="true">#</a> Gitlab Runner</h3><p>我们可以简单的把 Gitlab Runner 理解为 Gitlab 的一个插件，它负责执行 CI 流程，所以需要线安装它。</p><p>Gitlab Runner 可以部署在任何的服务器上，类似于 Jenkins master 是以容器方式运行，而 agent 是可以直接跑在宿主机上的。</p><p><img src="'+a+'" alt="jenkins-agent-master"></p><h4 id="安装-gitlab-runner"><a class="header-anchor" href="#安装-gitlab-runner" aria-hidden="true">#</a> 安装 Gitlab Runner</h4><p>这里我们直接使用 docker 安装。</p><div class="language-bash"><pre><code><span class="token comment"># 创建一个文件夹用于后续挂载 runner 的配置文件</span>\n<span class="token function">mkdir</span> -p /home/gitlab-runner/config\n<span class="token comment"># 拉取 gitlab-runner 镜像，尽量与 gitlab 版本保持一致</span>\n<span class="token function">docker</span> pull gitlab/gitlab-runner:v16.8.0\n<span class="token comment"># 查看 docker 镜像列表</span>\n<span class="token function">docker</span> images\n<span class="token comment"># 运行 gitlab-runner 镜像</span>\n<span class="token function">docker</span> run -itd --restart always --name gitlab-runner <span class="token punctuation">\\</span>\n-v /home/gitlab-runner/config:/etc/gitlab-runner <span class="token punctuation">\\</span>\n-v /var/run/docker.sock:/var/run/docker.sock <span class="token punctuation">\\</span>\ngitlab/gitlab-runner:v16.8.0\n<span class="token comment"># 查看运行的镜像</span>\n<span class="token function">docker</span> <span class="token function">ps</span> <span class="token operator">|</span> <span class="token function">grep</span> gitlab\n</code></pre></div><p>查看 docker 镜像，出现以下则说明 gitlab-runner 镜像运行成功。</p><p><img src="/_assets/gitlab-runner-installed.7428e597.png" alt="gitlab-runner-installed"></p><h4 id="注册-gitlab-runner"><a class="header-anchor" href="#注册-gitlab-runner" aria-hidden="true">#</a> 注册 Gitlab Runner</h4><p>gitlab-runner 容器启动完成后，还需要注册 gitlab-runner 才可使用。</p><div class="language-bash"><pre><code><span class="token comment"># 进入容器</span>\n<span class="token function">docker</span> <span class="token builtin class-name">exec</span> -it gitlab-runner-docker <span class="token function">sh</span>\n<span class="token comment"># 查看信息</span>\ngitlab-runner -v\n</code></pre></div><p><img src="/_assets/gitlab-runner-installed-v.42931c06.png" alt="gitlab-runner-installed-v"></p><p>开始注册：</p><div class="language-bash"><pre><code>gitlab-runner register\n</code></pre></div><p><img src="/_assets/gitlab-runner-register.ab63e212.png" alt="gitlab-runner-register"></p><p>我们按照步骤：</p><ol><li><p>输入 GitLab 地址和 token</p><p>直接从 CI/CD 页面，直接复制即可</p><p><img src="/_assets/gitlab-runner-register-gitlab.956d7deb.png" alt="gitlab-runner-register-gitlab"></p></li><li><p>输入 Runner 的描述信息</p><p>描述信息，也是 runner 的名称，此处填 <code>build</code>，也可不填，可以在 Gitlab 界面显示进行修改</p></li><li><p>输入 Tag</p><p>可不填，直接在 Gitlab 界面修改，可以用于指定在构建规定的 tag 时触发 CI，建议<strong>不要为纯数字，否则构建会报错</strong></p></li><li><p>输入用户权限备注</p><p>可不填</p></li><li><p>选择 Runner-Executor，建议 <code>docker</code></p></li><li><p>选择 Runner-Executor-Version，建议 <code>docker:latest</code></p><p><code>node:16.19-slim</code>，小版本，能省很多内存 根据提示填写 docker 执行器版本，后续作业以哪个镜像版本来运行 job（可在.gitlab-ci.yml 中修改需要的 image）</p></li></ol><p>所有步骤执行完会生成一个 Runner 的配置文件，默认位置是 <code>/etc/gitlab-runner/config.toml</code>，由于我们创建 Runner 容器时挂载了目录，该文件会同步出现在我们创建的本地挂载目录 <code>/home/gitlab-runner/config/config.toml</code> 中。</p><h4 id="执行器运行结果"><a class="header-anchor" href="#执行器运行结果" aria-hidden="true">#</a> 执行器运行结果</h4><div class="language-bash"><pre><code>gitlab-runner list\n</code></pre></div><p><img src="/_assets/gitlab-ci-runner-list.f594fbe6.png" alt="gitlab-ci-runner-list"></p><p>Runner 更多命令：</p><div class="language-bash"><pre><code>gitlab-runner register   <span class="token comment"># 交互式注册 Runner</span>\ngitlab-runner list       <span class="token comment"># 所有的 Runner 列表</span>\ngitlab-runner verify     <span class="token comment"># 检查注册的 Runner 是否可以连接</span>\ngitlab-runner unregister <span class="token comment"># 取消注册 Runner</span>\ngitlab-runner unregister --name test-runner\n</code></pre></div><p>此时可以返回仓库查看</p><p><img src="/_assets/gitlab-ci-setting-success.f892163d.png" alt="gitlab-ci-setting-success"></p><ul><li>页面新增一条可用的指定 Runner</li><li>如果指示灯是绿色则表示正常</li></ul><h3 id="再次测试流水线"><a class="header-anchor" href="#再次测试流水线" aria-hidden="true">#</a> 再次测试流水线</h3><p><img src="/_assets/gitlab-ci-run-pipeline.6a68c2ea.png" alt="gitlab-ci-run-pipeline"></p><p>点击 <code>Run pipeline</code> 开启一个新的流水线任务：</p><p>等待执行完成后，如下所示：</p><p><img src="/_assets/gitlab-ci-success.964c1d43.png" alt="gitlab-ci-success"></p><p>也可以点击进去查看详细信息，如我们的输出信息：</p><p><img src="/_assets/gitlab-ci-success-info.19e84b16.png" alt="gitlab-ci-success-info"></p><h3 id="遇到的问题"><a class="header-anchor" href="#遇到的问题" aria-hidden="true">#</a> 遇到的问题</h3><ol><li><p>使用 docker 拉取镜像的时候，报错 <code>missing signature key</code></p><div class="language-bash"><pre><code>yum <span class="token function">install</span> docker-ce -y\n<span class="token comment"># 重启docker</span>\nsystemctl restart <span class="token function">docker</span>\n</code></pre></div></li><li><p>gitlab-runner 连接成功，但作业一直是 <code>pending</code> 状态</p><p><img src="/_assets/gitlab-ci-untagged-jobs.3af2edad.png" alt="gitlab-ci-untagged-jobs"></p><p><code>Run untagged jobs</code> 项未勾选，表示此 runner 不能执行没有指定 tag 的 pipeline，勾选上即可</p></li><li><p>安装了低版本的 gitlab-runner</p><div class="language-bash"><pre><code><span class="token comment"># 删除容器</span>\n<span class="token function">docker</span> stop <span class="token operator">&lt;</span>container_id_or_name<span class="token operator">&gt;</span>\n<span class="token function">docker</span> <span class="token function">rm</span> <span class="token operator">&lt;</span>container_id_or_name<span class="token operator">&gt;</span>\n<span class="token comment"># 删除镜像</span>\n<span class="token function">docker</span> rmi <span class="token operator">&lt;</span>image_id_or_repository:tag<span class="token operator">&gt;</span>\n<span class="token comment"># 重新安装</span>\n</code></pre></div></li></ol><h2 id="docker"><a class="header-anchor" href="#docker" aria-hidden="true">#</a> Docker</h2><p>兄弟们，我终于弄懂 Docker 了 🎉，简直太好用了，对造出 Docker 的大神简直佩服的五体投地。</p><p>一直听别人说“Docker 好用”，“Docker 就是个虚拟机”，比如：后端项目部署时不仅包含自己的代码，还需要 mysql、redis、nginx 等服务，还有基础的 JDK 等等，这些东西还需要安装步骤、环境变量的设置等。</p><p>如果需要部署多台机器时，全都是重复工作，增加工作量的同时也增加了风险，万一哪一步漏掉了，服务就跑不起来了。</p><p>而 Docker 的出现就是为了解决这个问题，首先把上述说的一系列服务封装成成一个镜像，镜像运行起来就是一个容器（容器相当于一个虚拟机，并且帮你安装好了上述的基础服务），可以一次运行多个容器，所以它的 logo 是这样式的。</p><p><img src="/_assets/docker-logo.3a1da4b9.png" alt="docker-logo.png"></p><p>并且 Docker 提供了端口和文件映射，我们只需要把容器的端口映射到宿主机，就可以访问到容器内的服务了。</p><p><img src="/_assets/docker-images-containers.a985dee4.png" alt="docker-images-containers"></p><p>没上手前感觉 Docker 离我好远，“这是个啥呀，我不会所以我不用”；用了一次，呀！真香！</p><p>安装 Docker 环境就不说了，网上一搜一大把，<strong>强烈推荐：安装桌面端 Docker</strong>。</p><p><a href="https://www.docker.com/products/docker-desktop/" target="_blank" rel="noopener noreferrer">👇 下载地址</a></p><h3 id="重要概念"><a class="header-anchor" href="#重要概念" aria-hidden="true">#</a> 重要概念</h3><p>如果已经安装完 Docker，首先拉取一个镜像。</p><div class="language-bash"><pre><code><span class="token function">docker</span> pull nginx\n</code></pre></div><p><img src="/_assets/docker-pull-code.4fd1b533.png" alt="docker-pull-code"></p><p>或者直接在桌面端 Docker 中搜索，并拉取：</p><p><img src="/_assets/docker-pull-desktop.6dae5aa1.png" alt="docker-pull-desktop"></p><p>接着看几个 Docker 中经常会提到的名词，了解这几个名词的关系，有助于我们更好的掌握 Docker。</p><p>哦，你还不懂 Docker 命令，没事，直接在终端输入 docker，提示就出来了。</p><p><img src="/_assets/docker-bash-code.c6df448a.png" alt="docker-bash-code"></p><h4 id="images-镜像"><a class="header-anchor" href="#images-镜像" aria-hidden="true">#</a> images 镜像</h4><p>images 就是所有镜像的列表，可以通过 pull 拉取更多镜像，可以通过 <code>docker images</code> 命令查看：</p><p><img src="/_assets/docker-images-code.13c8552b.png" alt="docker-images-code"></p><p>或者直接在桌面端看：</p><p><img src="/_assets/docker-images-desktop.867e9cb5.png" alt="docker-images-desktop"></p><h4 id="containers-容器"><a class="header-anchor" href="#containers-容器" aria-hidden="true">#</a> containers 容器</h4><p>containers 就是镜像跑起来的容器，<strong>容器是镜像的一个实例，一个镜像可以跑多个容器</strong></p><p><img src="/_assets/docker-containers-runs.b02c9279.png" alt="docker-containers-runs"></p><p>点击 <code>Run</code> 就弹出一个表单，填写如下内容：</p><ul><li>容器名称：不填写，随机生成名字</li><li>端口映射：就是把容器的端口映射到宿主机的端口上，比如容器内 nginx 服务是 80 端口，需要把宿主机的端口映射到这个端口，在宿主机上才可以访问 nginx 服务</li><li>数据卷：就是把宿主机某个目录挂载到容器内，可以设置多个，如保存日志的、配置文件、nginx 的 html 目录等，这样容器内的数据就会与宿主机同步，相当于将容器内的数据保存到宿主机了</li><li>环境变量：就是容器的环境变量，可以设置多个，如数据库连接信息、环境变量等</li></ul><p>在桌面端 Docker 中，可以看到当前运行的容器列表：</p><p><img src="/_assets/docker-containers-list.a1f94d1c.png" alt="docker-containers-list"></p><p>通过 <code>docker ps</code> 也可以查看当前启动的容器列表：</p><p><img src="/_assets/docker-containers-list1.2539c3aa.png" alt="docker-containers-list1"></p><p>这里我启动 4 个容器：</p><ol><li>直接 <code>Run</code>，没有任何配置</li><li>配置了名称</li><li>配置名称和端口</li><li>配置名称、端口和文件挂载路径</li></ol><p><img src="/_assets/docker-containers-mount.9c54a9aa.png" alt="docker-containers-mount"></p><p>浏览器打开 <a href="http://localhost:8091/" target="_blank" rel="noopener noreferrer">http://localhost:8091/</a>，显示我们本地的 html 文件内容</p><p><img src="/_assets/docker-containers-mount1.c74e345c.png" alt="docker-containers-mount1"></p><p>上述四个容器，也可以通过 <code>docker run</code> 命令行启动，格式为：</p><div class="language-bash"><pre><code><span class="token comment"># --name 设置容器名称</span>\n<span class="token comment"># -p 端口映射</span>\n<span class="token comment"># -v 指定数据卷挂载目录，可多次设置</span>\n<span class="token comment"># -e 指定环境变量，Key=Value</span>\n<span class="token comment"># -d 后台运行</span>\n<span class="token function">docker</span> run --name xxx -p xx:xx -v xx:xx -e <span class="token assign-left variable">xx</span><span class="token operator">=</span>xx -d nginx:latest\n</code></pre></div><p>那么，我们刚刚启动的 4 个容器，分别对应的命令就是：</p><ol><li><code>docker run nginx:latest</code></li><li><code>docker run --name nginx1 nginx:latest</code></li><li><code>docker run --name nginx2 -p 8090:80 nginx:latest</code></li><li><code>docker run --name nginx3 -p 8091:80 -v /Users/Desktop/html:/usr/share/nginx/html nginx:latest</code></li></ol><h4 id="volumes"><a class="header-anchor" href="#volumes" aria-hidden="true">#</a> volumes</h4><p>上面已经提到了，数据卷 volume，用来把宿主机某个目录挂载到容器内。</p><p>理解了以上几个概念，Docker 也就算入门了。我们来梳理一下流程：首先需要拉取一个镜像，然后通过镜像运行容器，将容器内服务的端口映射到宿主机，就可以访问容器内的服务了，再将对应的目录挂载到宿主机，这样容器内的数据就会与宿主机同步，相当于将容器内的数据保存到宿主机。</p><p>上面我们讲了如何使用镜像、容器的过程，在项目里我们不仅仅是使用它，而且还需要用它来帮我们部署多套环境，比如将我们的服务生成 Docker 镜像。那么，如何生成 Docker 镜像呢？</p><h3 id="如何生成-docker-镜像"><a class="header-anchor" href="#如何生成-docker-镜像" aria-hidden="true">#</a> 如何生成 Docker 镜像</h3><p><code>Dockerfile</code> 是生成 Docker 镜像的文件，在 <code>docker run</code> 的时候执行。</p><p>以我们的真实项目为例，首先在项目根目录创建一个 <code>Dockerfile</code> 和 <code>.dockerignore</code> 文件，</p><p><img src="/_assets/qtable-coll-excel.8190ba50.png" alt="qtable-coll-excel"></p><p><code>Dockerfile</code> 内容如下：</p><div class="language-bash"><pre><code>FROM node:16\nWORKDIR /usr/src\nCOPY <span class="token builtin class-name">.</span> ./\nRUN <span class="token function">npm</span> <span class="token function">install</span> --registry<span class="token operator">=</span>https://registry.npmmirror.com --ignore-scripts\nCMD <span class="token builtin class-name">export</span> <span class="token assign-left variable">PRODUCTION</span><span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">;</span> <span class="token function">node</span> bin/server.js\n</code></pre></div><p>这些指令的含义如下：</p><ul><li><p>FROM：基于一个基础镜像来修改，我们项目是一个 node 项目，所以需要基于 node16 来创建镜像</p></li><li><p>WORKDIR：指定当前工作目录</p></li><li><p>COPY：把容器外的内容复制到容器内</p><p>通过 <code>WORKDIR</code> 指定当前工作目录为 <code>/usr/src</code>，接着通过 <code>COPY</code> 把 Dockerfile 同级目录下的内容复制到容器内的 <code>/usr/src</code> 目录下</p></li><li><p>RUN：在容器内执行命令，安装依赖</p></li><li><p>CMD：启动容器执行的命令</p></li></ul><p>.dockerignore 类似于 .gitignore，在 docker build 时，.gitignore 下的文件会被忽略，不会被打包到 Docker 镜像中。</p><p>接着我们就可以使用 docker build 来制作镜像了，语法如下：</p><div class="language-bash"><pre><code><span class="token comment"># docker build -t 镜像名:标签名 Dockerfile路径</span>\n<span class="token function">docker</span> build -t doc-socket-excel:0.0.3 <span class="token builtin class-name">.</span>\n<span class="token comment"># 也可以不需要标签名</span>\n<span class="token comment"># docker build -t doc-socket-excel .</span>\n</code></pre></div><p>通过输出可以看到，首先拉取了 node16 镜像，接着拉取我们的代码并执行命令。</p><p><img src="/_assets/docker-make-image.a850512d.png" alt="docker-make-image"></p><p>通过 docker images 看到我们的 docker 镜像，通过 docker run 运行容器，通过 docker ps 查看当前运行的容器</p><p><img src="/_assets/docker-run-image.b8a5004d.png" alt="docker-run-image"></p><p>接着可以使用 <code>docker exec -it doc-socket-excel sh</code> 进入到容器内部，在对应的目录下（之前执行的<code>WORKDIR</code>）就能看到我们的项目代码。</p><p>最后通过挂载的 logs 目录就能看到我们的项目日志。</p><p><img src="/_assets/docker-image-logs.55cc14cf.png" alt="docker-image-logs"></p><h2 id="gitlab-ci-yml-文件编写"><a class="header-anchor" href="#gitlab-ci-yml-文件编写" aria-hidden="true">#</a> .gitlab-ci.yml 文件编写</h2><p><a href="https://git.qmpoa.com/help/ci/yaml/index" target="_blank" rel="noopener noreferrer">语法</a></p><div class="language-yml"><pre><code><span class="token key atrule">variables</span><span class="token punctuation">:</span> <span class="token comment"># 定义全局变量 </span>\n    PROJECT_IMAGES<span class="token punctuation">:</span> <span class="token string">&quot;irweb:$CI_COMMIT_TAG&quot;</span> <span class="token comment"># gitlab系统变量，获取提交tag的信息</span>\n</code></pre></div>',122)];o.render=function(s,a,t,o,p,l){return n(),e("div",null,c)};export{t as __pageData,o as default};
