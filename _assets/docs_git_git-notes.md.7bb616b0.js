import{f as e,g as a,J as t}from"./common-03e46d7f.js";import{_ as s}from"./common-d0f13562.js";const n='{"title":"git使用技巧","frontmatter":{"date":"2022-08-03","title":"git使用技巧","tags":["git"],"describe":"整理了一些自己在工作中用到的git命令及案例"},"headers":[{"level":3,"title":"git clone","slug":"git-clone"},{"level":3,"title":"git log","slug":"git-log"},{"level":3,"title":"git commit --amend","slug":"git-commit-amend"},{"level":3,"title":"git stash","slug":"git-stash"},{"level":3,"title":"git merge 合并","slug":"git-merge-合并"},{"level":3,"title":"合并冲突","slug":"合并冲突"},{"level":3,"title":"变基（Rebasing）","slug":"变基（rebasing）"},{"level":3,"title":"交互式变基（interactive Rebase）","slug":"交互式变基（interactive-rebase）"},{"level":3,"title":"重置（Resetting）","slug":"重置（resetting）"},{"level":3,"title":"还原（Reverting）","slug":"还原（reverting）"},{"level":3,"title":"精选（Cherry-picking）","slug":"精选（cherry-picking）"},{"level":3,"title":"取回（Fetching）","slug":"取回（fetching）"},{"level":3,"title":"拉取（Pulling）","slug":"拉取（pulling）"},{"level":3,"title":"git reflog","slug":"git-reflog"},{"level":3,"title":"git branch","slug":"git-branch"},{"level":3,"title":"git tag","slug":"git-tag"},{"level":3,"title":"配置别名","slug":"配置别名"}],"relativePath":"docs/git/git-notes.md","lastUpdated":1743566425474.403}';var i={};const o=[t('<h3 id="git-clone"><a class="header-anchor" href="#git-clone" aria-hidden="true">#</a> git clone</h3><p>工作区 add 暂存区 commit 本地仓库 push -&gt; &lt;- clone 远程仓库 拉取指定分支代码 <code>git clone -b dev https://xxx.git</code></p><h3 id="git-log"><a class="header-anchor" href="#git-log" aria-hidden="true">#</a> git log</h3><div class="language-bash"><pre><code><span class="token function">git</span> reset --hard HEAD~1 <span class="token comment"># 回退到上一个版本</span>\n<span class="token function">git</span> log --pretty<span class="token operator">=</span>oneline <span class="token comment"># 使用git log看不到reset的操作</span>\n<span class="token function">git</span> reflog <span class="token comment"># git reflog可以看到reset的操作</span>\n<span class="token function">git</span> reset  --hard 9039b57 <span class="token comment"># 然后就可以重新恢复</span>\n</code></pre></div><h3 id="git-commit-amend"><a class="header-anchor" href="#git-commit-amend" aria-hidden="true">#</a> git commit --amend</h3><p>amend 原意就是&quot;修正&quot;的意思，工作中常用到以下几种</p><ul><li><p>只修正文件，不修正提交信息，如提交的时候发现有文件忘记提交，先添加到暂存区，在使用下面的命令进行修正，之后就可以看到提交中已经有了忘记提交的文件 <code>git commit --amend --no-edit</code><img src="/blog/_assets/famend.62bbfe67.jpg" alt="famend"> 如果多提交了文件，也可以先通过<code>git rm --cached &lt;文件名&gt;</code>，再通过以上命令修正 <img src="/blog/_assets/rm.1813e0fe.jpg" alt="rm"></p></li><li><p>只修正提交信息，如提交时发现写的提交信息不太正确时，可通过以下命令修改 <code>git commit --amend -m &quot;feat: ceshi&quot;</code><img src="/blog/_assets/amend.7580958b.jpg" alt="amend"></p></li><li><p>修改提交信息和文件 <code>git commit --amend</code></p></li></ul><h3 id="git-stash"><a class="header-anchor" href="#git-stash" aria-hidden="true">#</a> git stash</h3><div class="language-bash"><pre><code><span class="token function">git</span> stash <span class="token comment"># 暂存当前正在进行的工作， 比如想 pull 最新代码， 又不想加新 commit，或者为了 fix 一个紧急的 bug, 先 stash，使返回到自己上一个 commit， 改完 bug 之后再 stash pop, 继续原来的工作</span>\n<span class="token function">git</span> stash save <span class="token string">&quot;message&quot;</span> <span class="token comment"># 暂存时加备注 方便查找</span>\n<span class="token function">git</span> stash show <span class="token comment"># 默认显示第一个改动 如果显示其他 git stash show &quot;stash@{1}&quot;</span>\n<span class="token function">git</span> stash show -p <span class="token comment"># 改动的具体</span>\n<span class="token function">git</span> stash apply <span class="token comment"># 恢复第一个存储 恢复其他使用 git stash apply &quot;stash@{1}&quot;</span>\n<span class="token function">git</span> stash drop <span class="token string">&quot;stash@{1}&quot;</span> <span class="token comment"># 删除第一个存储</span>\n<span class="token function">git</span> pop <span class="token comment"># git apply 和 drop 结合体</span>\n<span class="token function">git</span> stash clect <span class="token comment"># 清空</span>\n</code></pre></div><h3 id="git-merge-合并"><a class="header-anchor" href="#git-merge-合并" aria-hidden="true">#</a> git merge 合并</h3><ul><li><p>Fast-forward (-ff)</p><p>当前分支相比于要合并的分支没有额外的提交时，执行 fast-forward 合并，这类合并不会创建新的提交，而是会将我们正在合并的分支上的提交直接合并到当前分支</p></li><li><p>No-fast-forward (--no-ff)</p><p>如果当前分支具有要合并的分支不具备的改变时，会执行 no-fast-forward 合并，会在当前活动分支上创建 merging commit，这个提交的父提交(parent commit) 既指向这个活动分支，也指向我们想要合并的分支</p></li></ul><h3 id="合并冲突"><a class="header-anchor" href="#合并冲突" aria-hidden="true">#</a> 合并冲突</h3><p>当合并出现冲突时，git 会展示冲突出现的位置，可以手动修改，再次添加已修改的文件，并提交</p><h3 id="变基（rebasing）"><a class="header-anchor" href="#变基（rebasing）" aria-hidden="true">#</a> 变基（Rebasing）</h3><p>通过 git merge 可以将一个分支的修改应用到另一个分支，git rebase 可以将一个分支的修改融入到另一个分支</p><p>git rebase 将当前分支的提交复制到指定分支</p><p>如当前在 dev 分支，使用<code>git rebase master</code>，可以在 dev 分支上获取 master 上的所有修改</p><p>变基与合并最大的区别：Git 不会尝试确定要保留或不保留哪些文件。使用 rebase 不会遇到合并冲突，而且可以保留漂亮的、线性的 git 历史记录</p><p>使用：如果在开发一个 feature 分支并且 master 分支已经更新过，就可以使用 rebase 在此分支获取所有更新，防止未来出现合并冲突</p><p><a href="https://blog.csdn.net/wh_19910525/article/details/7554489" target="_blank" rel="noopener noreferrer">git mere 和 git rebase 小结</a></p><p>git rebase 与 git merge 对比</p><p>假如有两个分支</p><div class="language-bash"><pre><code>       D---E <span class="token builtin class-name">test</span>\n      /\n A---B---C---F--- master\n</code></pre></div><p>git merge 后生成</p><div class="language-bash"><pre><code>       D--------E\n      /          <span class="token punctuation">\\</span>\n A---B---C---F----G---   test, master\n</code></pre></div><p>git rebase 后生成</p><div class="language-bash"><pre><code>A---B---D---E---C<span class="token string">&#39;---F&#39;</span>---   test, master\n</code></pre></div><p>merge 操作会生成一个新的节点，之前的提交分开显示。 而 rebase 操作不会生成新的节点，是将两个分支融合成一个线性的提交，不会增加新的提交。</p><h3 id="交互式变基（interactive-rebase）"><a class="header-anchor" href="#交互式变基（interactive-rebase）" aria-hidden="true">#</a> 交互式变基（interactive Rebase）</h3><p>在为提交执行变基之前，我们可以使用交互式变基修改它们，交互式变基在当前开发的分支上以及想要修改某些提交时有用</p><p>在我们正在 rebase 的提交上，可以执行以下 6 个动作：</p><ul><li>reword：修改提交信息</li><li>edit：修改此提交</li><li>squash：将提交融合到前一个提交中</li><li>fixup：将提交融合到前一个提交中，不保留该提交的日志信息</li><li>exec：在每个提交上运行我们想要的 rebase 命令</li><li>drop：移除该提交</li></ul><h3 id="重置（resetting）"><a class="header-anchor" href="#重置（resetting）" aria-hidden="true">#</a> 重置（Resetting）</h3><p>git reset 能让我们不在使用台面上的文件，控制 HEAD 应该指向的位置</p><ul><li><p>软重置</p><p>假如已经 commit 的文件有问题，想撤销提交，但又想保留文件，这时候就可以使用<code>git reset --soft HEAD~1</code>将 HEAD 指向前一次提交，通过 git status 可以看到这些文件，然后可以重新修复并提交</p></li><li><p>硬重置</p><p>有时候并不想保留特定提交引入的修改，Git 应该直接将整体状态重置到特定提交之前的状态，使用<code>git reset --hard HEAD~1</code></p></li></ul><h3 id="还原（reverting）"><a class="header-anchor" href="#还原（reverting）" aria-hidden="true">#</a> 还原（Reverting）</h3><p>另一种撤销修改的方式是执行 git revert，对特定的提交执行还原操作</p><p>假如某个提交添加了一个引用文件 index.js，但之后发现不再需要这个文件，那么就可以使用<code>git revert 2175894</code>还原那个提交，而且也不会修改分支的历史</p><h3 id="精选（cherry-picking）"><a class="header-anchor" href="#精选（cherry-picking）" aria-hidden="true">#</a> 精选（Cherry-picking）</h3><p>当一个特定分支包含当前活动分支所需的某个提交时，可以对那个提交进行 cherry-pick，此时会在当前分支上创建一个新的提交，包含由精选出来的提交所引入的修改</p><p>假如 dev 上 index.js 文件添加一项修改，希望整合到 master，但又不想合并整个 dev 分支，使用<code>git cherry-pick 2175894</code>引入特定修改</p><h3 id="取回（fetching）"><a class="header-anchor" href="#取回（fetching）" aria-hidden="true">#</a> 取回（Fetching）</h3><p>fetch 只是单纯下载新的变动</p><h3 id="拉取（pulling）"><a class="header-anchor" href="#拉取（pulling）" aria-hidden="true">#</a> 拉取（Pulling）</h3><p><code>git pull</code>实际上是两个命令：<code>git fetch</code>和<code>git merge</code>，拉取最新变动并自动合并到本地分支</p><p><code>git pull --rebase</code> 变基式合并</p><h3 id="git-reflog"><a class="header-anchor" href="#git-reflog" aria-hidden="true">#</a> git reflog</h3><p>每个人都会犯错，但犯错其实没啥！有时候可能感觉你把 git repo 完全搞坏，让你想完全删了了事。</p><p><code>git reflog</code>可以展示已经执行过的所有动作的日志，包括合并、重置、还原，包含你对分支所作的所有修改，如果犯了错，可以通过重置 HEAD 来轻松重做</p><p>假如我们合并 dev 到 master，但是并不需要这次合并，当执行<code>git reflog</code>后，可以看到 repo 状态合并前位于 HEAD@{1}，执行<code>git reset HEAD@{1}</code>，将 HEAD 重新指向 HEAD@{1}的位置。</p><h3 id="git-branch"><a class="header-anchor" href="#git-branch" aria-hidden="true">#</a> git branch</h3><p>分支操作</p><div class="language-bash"><pre><code><span class="token comment"># 删除本地分支 -D指不用管被删除的分支是否已合并</span>\n<span class="token function">git</span> branch -d xxx\n<span class="token function">git</span> branch -D xxx\n<span class="token comment"># 删除远程分支</span>\n<span class="token function">git</span> push origin --delete xxx\n</code></pre></div><h3 id="git-tag"><a class="header-anchor" href="#git-tag" aria-hidden="true">#</a> git tag</h3><div class="language-bash"><pre><code><span class="token function">git</span> tag <span class="token comment"># 在控制台打印出当前仓库的所有标签</span>\n<span class="token comment"># push 单个 tag，命令格式为：git push origin [tagname]</span>\n<span class="token comment"># 例如： git push origin v1.0</span>\n<span class="token function">git</span> push origin --tags  <span class="token comment">#  所有 tag</span>\n<span class="token function">git</span> tag <span class="token number">1.4</span>.10 50435cd <span class="token comment">#  指定 commit 打 tag</span>\n<span class="token function">git</span> tag -d xxx  <span class="token comment">#  删除本地 tag</span>\n</code></pre></div><h3 id="配置别名"><a class="header-anchor" href="#配置别名" aria-hidden="true">#</a> 配置别名</h3><div class="language-bash"><pre><code><span class="token function">git</span> config --global alias.st status\n<span class="token function">git</span> config --global alias.br branch\n<span class="token function">git</span> config --global alias.ci commit\n<span class="token function">git</span> config --global alias.co checkout\n<span class="token function">git</span> config --global alias.lg <span class="token string">&quot;log --color --graph --pretty=format:&#39;%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset&#39; --abbrev-commit&quot;</span>\n</code></pre></div><p>加入 global 表示对修改全局，如果不加，只对当前仓库起作用</p><p>配置文件.git/config 中</p><p>别名就在<code>[alias]</code>后面，要删除别名，直接把对应的行删掉即可</p><p>全局 Git 配置文件放在用户主目录下的一个隐藏文件<code>.gitconfig</code>中</p><h4 id="fork-项目-更新"><a class="header-anchor" href="#fork-项目-更新" aria-hidden="true">#</a> fork 项目--更新</h4><p>Fork 别人的项目后，如何再同步更新别人的提交</p><ol><li><p>添加远程仓库</p><p><img src="/blog/_assets/origin.6a4c5442.jpg" alt="origin"></p></li><li><p>拉取远程分支代码</p><p><img src="/blog/_assets/pull.8c020a80.jpg" alt="pull"></p></li><li><p>合并本地代码</p><div class="language-bash"><pre><code><span class="token function">git</span> merge upstream/master\n</code></pre></div></li><li><p>推送到 fork 的远程分支</p><div class="language-bash"><pre><code><span class="token function">git</span> push origin master\n</code></pre></div></li></ol><p>附一张 git 常用指令图</p><p><img src="'+s+'" alt="git"></p>',66)];i.render=function(t,s,n,i,l,c){return e(),a("div",null,o)};export{n as __pageData,i as default};
