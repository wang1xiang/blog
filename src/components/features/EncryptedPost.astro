---
import EncryptedContent from "./EncryptedContent.astro";

interface Props {
	password: string;
	slug: string;
	hint?: string;
}
---

<EncryptedContent {...Astro.props}>
  <slot />
</EncryptedContent>
