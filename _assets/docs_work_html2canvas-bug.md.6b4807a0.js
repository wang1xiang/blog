import{f as n,g as a,J as s}from"./common-03e46d7f.js";const t='{"title":"悟了，html2canvas截图原理，它到底是怎么截图的？","frontmatter":{"date":"2023-08-21","title":"悟了，html2canvas截图原理，它到底是怎么截图的？","tags":["work"],"describe":"为了验证自己的猜想，浅浅调试一下 html2canvas 的源码，看下 html2canvas 是怎样一个流程，它是如何将 html内转成 canvas 的。"},"headers":[{"level":2,"title":"🐛 问题复现","slug":"🐛-问题复现"},{"level":2,"title":"调试开始","slug":"调试开始"},{"level":3,"title":"renderElement 执行流程","slug":"renderelement-执行流程"},{"level":3,"title":"克隆原始 DOM","slug":"克隆原始-dom"},{"level":3,"title":"绘制 canvas","slug":"绘制-canvas"},{"level":2,"title":"针对此 Bug 分析","slug":"针对此-bug-分析"},{"level":2,"title":"解决方案","slug":"解决方案"},{"level":3,"title":"foreignObjectRendering","slug":"foreignobjectrendering"},{"level":3,"title":"对包含背景色的内联标签截断处理","slug":"对包含背景色的内联标签截断处理"},{"level":2,"title":"总结","slug":"总结"}],"relativePath":"docs/work/html2canvas-bug.md","lastUpdated":1742176930618.5994}';var e={};const p=[s('<h2 id="🐛-问题复现"><a class="header-anchor" href="#🐛-问题复现" aria-hidden="true">#</a> 🐛 问题复现</h2><p>之前在做 html 内容导出为 pdf、图片时，我用的是 html2canvas 生成截屏，再进一步转换为 pdf 文件，感兴趣的同学可以看下这篇<a href="https://juejin.cn/post/7220434734966571068" target="_blank" rel="noopener noreferrer">一文搞定前端 html 内容转图片、pdf 和 word 等文件</a>，截图得到的图片内容、质量都没有什么问题。</p><p>不过最近有个同事反应，他导出的图片有 bug，这我倒挺好奇的，因为这个导出功能已经用了很久，并没有人反馈过有问题（除了那个 <strong>pdf 翻页内容被截断</strong>的问题，求助 jym 🙏：前端有好的解决方法吗？），于是我要了他的文档，果不其然，出现了下面红框所示的问题。</p><p><img src="/blog/_assets/html2canvas-export-png.41f26764.png" alt="html2canvas-export-png"></p><p>检查一下它的 DOM 结构，发现是下面这样，猜测是就是这个原因导致的。</p><p><img src="/blog/_assets/html2canvas-mark-bug.d8b06493.png" alt="html2canvas-mark-bug"></p><p>为了验证自己的猜想，浅浅调试一下 html2canvas 的源码，看下 html2canvas 是怎样一个流程，它是如何将 html 内转成 canvas 的。</p><h2 id="调试开始"><a class="header-anchor" href="#调试开始" aria-hidden="true">#</a> 调试开始</h2><p>我们在 html2canvas 执行的地方打个断点，开始调试代码：</p><p><img src="/blog/_assets/html2canvas-point.528501d7.png" alt="html2canvas-point.png"></p><p>进入 html2canvas 内部，可以看到内部执行的是 renderElement 方法：</p><p><img src="/blog/_assets/html2canvas-renderElement.4b67604a.png" alt="html2canvas-renderElement"></p><p>我们直接进入到 renderElement 方法内部，看下它的执行流程：</p><h3 id="renderelement-执行流程"><a class="header-anchor" href="#renderelement-执行流程" aria-hidden="true">#</a> renderElement 执行流程</h3><h4 id="判断传入节点是否有效"><a class="header-anchor" href="#判断传入节点是否有效" aria-hidden="true">#</a> 判断传入节点是否有效</h4><p><img src="/blog/_assets/html2canvas-validate-element.7f6460d9.png" alt="html2canvas-validate-element"></p><ul><li><code>Node.ownerDocument</code>：只读属性，返回当前节点的顶层的 document 对象；</li><li><code>document.defaultView</code>：属性返回当前 document 对象所关联的 window 对象，如果没有，会返回 null。</li></ul><p>这里主要判断节点，快速跳过，继续执行 👇。</p><h4 id="合并配置项"><a class="header-anchor" href="#合并配置项" aria-hidden="true">#</a> 合并配置项</h4><p>将用户传入的 options 与默认的 options 合并</p><p><img src="/blog/_assets/html2canvas-merge-options.d2d7fa96.png" alt="html2canvas-merge-options"></p><p>构建配置项，将传入的 <code>opts</code> 与默认配置合并，同时初始化一个 <code>context</code> 上下文对象（缓存、日志等）：</p><p><img src="/blog/_assets/html2canvas-context.97b02f26.png" alt="html2canvas-context.png"></p><h5 id="缓存对象-cache"><a class="header-anchor" href="#缓存对象-cache" aria-hidden="true">#</a> 缓存对象 cache</h5><p>其中 <code>cache</code> 为缓存对象，主要是<strong>避免资源重复加载</strong>的问题。</p><p>原理如下：</p><p>如果遇到图片链接为 <code>blob</code>，在加载完成后，会添加到缓存 <code>_cache</code> 中：</p><p><img src="/blog/_assets/html2canvas-cache.048dd8b6.png" alt="html2canvas-cache"></p><p>下次使用直接通过 <code>this._cache[src]</code> 从缓存中获取，不用再发送请求：</p><p>同时，<code>cache</code> 中<strong>控制图片的加载和处理</strong>，包括使用 <code>proxy</code> 代理和使用 <code>cors</code> 跨域资源共享这两种情况资源的处理。</p><p>继续往下执行 👇</p><h3 id="克隆原始-dom"><a class="header-anchor" href="#克隆原始-dom" aria-hidden="true">#</a> 克隆原始 DOM</h3><p>使用 DocumentCloner 方法克隆原始 DOM，避免修改原始 DOM。</p><p><img src="/blog/_assets/html2canvas-generate-iframe.15918af3.png" alt="html2canvas-generate-iframe"></p><p>使用 clonedReferenceElement 将原始 DOM 进行克隆，并调用 toIFrame 将克隆到的 DOM 绘制到 iframe 中进行渲染，此时在 DOM 树中会出现 class 为 html2canvas-toIFrame 的 iframe 节点，通过 <code>window.getComputedStyle</code> 就可以拿到要克隆的目标节点上所有的样式了。</p><p><img src="/blog/_assets/html2canvas-toIFrame.5f5cfbdf.png" alt="html2canvas-toIFrame"></p><h3 id="绘制-canvas"><a class="header-anchor" href="#绘制-canvas" aria-hidden="true">#</a> 绘制 canvas</h3><p>前面几步很简单，主要是对传入的 DOM 元素进行解析，获取目标节点的样式和内容。重点是 <code>toCanvas</code> 即将 DOM 渲染为 canvas 这个过程，html2canvas 提供了两种绘制 canvas 的方式：</p><ol><li>使用 foreignObject 方式绘制 canvas</li><li>使用纯 canvas 方法绘制</li></ol><p>咱们接着执行，当代码执行到这里时判断是否使用 foreignObject 的方式生成 canvas：</p><p><img src="/blog/_assets/html2canvas-foreignObjectRendering.dc5a4897.png" alt="html2canvas-foreignObjectRendering"></p><h4 id="使用-foreignobject-方式绘制-canvas"><a class="header-anchor" href="#使用-foreignobject-方式绘制-canvas" aria-hidden="true">#</a> 使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/SVG/Element/foreignObject" target="_blank" rel="noopener noreferrer">foreignObject</a> 方式绘制 canvas</h4><p>首先了解下 foreignObject 是什么？</p><ol><li><p>svg 中的<code>xmlns</code> 全称是“XML Namespaces”，即 XML 命名空间，正是它的存在，在浏览器中 svg 才能正常渲染（下面这段代码是在 iconfont 上随便复制的一个 icon svg 代码）；</p><div class="language-xml"><pre><code> <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>svg</span> <span class="token attr-name">t</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>1692613183565<span class="token punctuation">&quot;</span></span> <span class="token attr-name">class</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>icon<span class="token punctuation">&quot;</span></span> <span class="token attr-name">viewBox</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>0 0 1024 1024<span class="token punctuation">&quot;</span></span> <span class="token attr-name">version</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>1.1<span class="token punctuation">&quot;</span></span> \t\t<span class="token attr-name">xmlns</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>http://www.w3.org/2000/svg<span class="token punctuation">&quot;</span></span> <span class="token attr-name">p-id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>20058<span class="token punctuation">&quot;</span></span> <span class="token attr-name">width</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>200<span class="token punctuation">&quot;</span></span> <span class="token attr-name">height</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>200<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>path</span> <span class="token attr-name">d</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>M548.571429 292.571429v182.784L731.428571 475.428571v73.142858l-182.857142-0.073143V731.428571h-73.142858V548.498286L292.571429 548.571429v-73.142858l182.857142-0.073142V292.571429h73.142858z<span class="token punctuation">&quot;</span></span> <span class="token attr-name">fill</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>#626B7D<span class="token punctuation">&quot;</span></span> <span class="token attr-name">p-id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>20059<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>path</span><span class="token punctuation">&gt;</span></span>\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>svg</span><span class="token punctuation">&gt;</span></span>\n</code></pre></div></li><li><p>foreignObject 允许包含来自不同的 XML 命名空间的元素，借助 <code>&lt;foreignObject&gt;</code> 标签，我们可以直接将 DOM 节点作为 foreignObject 插入 SVG 节点中进行渲染，如下：</p><div class="language-xml"><pre><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>svg</span> <span class="token attr-name">xmlns</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>http://www.w3.org/2000/svg<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>foreignObject</span> <span class="token attr-name">width</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>120<span class="token punctuation">&quot;</span></span> <span class="token attr-name">height</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>50<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n     <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>body</span> <span class="token attr-name">xmlns</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>http://www.w3.org/1999/xhtml<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n       <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span><span class="token punctuation">&gt;</span></span>测试测试<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">&gt;</span></span>\n     <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>body</span><span class="token punctuation">&gt;</span></span>\n   <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>foreignObject</span><span class="token punctuation">&gt;</span></span>\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>svg</span><span class="token punctuation">&gt;</span></span>\n</code></pre></div><p>可以看到 <code>&lt;foreignObject&gt;</code> 标签里面有一个设置了 <code>xmlns=&quot;http://www.w3.org/1999/xhtml&quot;</code> 命名空间的 <code>&lt;body&gt;</code> 标签，此时 <code>&lt;body&gt;</code> 标签及其子标签都会按照 XHTML 标准渲染，实现了 SVG 和 XHTML 的混合使用。</p><p>这样只需要指定对应的命名空间，就可以把它嵌套到 foreignObject 中，然后结合 SVG，直接渲染。</p><p>对于不同的命名空间，浏览器解析的方式也不一样，所以在 SVG 中嵌套 HTML，解析 SVG 的时候遇到 <code>http://www.w3.org/2000/svg</code> 转化 SVG 的解析方式，当遇到了 <code>http://www.w3.org/1999/xhtml</code> 就使用 HTML 的解析方式。</p><p>这是为什么 SVG 中可以嵌套 HTML，并且浏览器能够正常渲染。</p></li><li><p>foreignObject 已经支持除 IE 外的主流浏览器</p><p><img src="/blog/_assets/can-i-use-foreignObject.99ce1db8.png" alt="can-i-use-foreignObject.png"></p></li></ol><p>弄懂 foreignObject 后，我们尝试将 <code>foreignObjectRendering</code> 设置为 <code>true</code>，看看它是如何生成 canvas 的：</p><div class="language-js"><pre><code><span class="token function">Html2canvas</span><span class="token punctuation">(</span>warp<span class="token punctuation">,</span> <span class="token punctuation">{</span>\n  <span class="token literal-property property">useCORS</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span>\n  <span class="token literal-property property">foreignObjectRendering</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span>\n</code></pre></div><p>在此处打个断点：</p><p><img src="/blog/_assets/html2canvas-use-foreignObjectRendering.6554810e.png" alt="html2canvas-use-foreignObjectRendering"></p><p>进入 ForeignObjectRenderer 类中</p><p><img src="/blog/_assets/html2canvas-ForeignObjectRenderer.8a3e3693.png" alt="html2canvas-ForeignObjectRenderer"></p><p>通过 ForeignObjectRenderer 实例化一个 renderer 渲染器实例，在 ForeignObjectRenderer 构造方法中初始化 <code>this.canvas</code> 对象及其上下文 <code>this.ctx</code></p><p><img src="/blog/_assets/html2canvas-ForeignObjectRenderer-render.2bd58170.png" alt="html2canvas-ForeignObjectRenderer-render"></p><p>调用 render 生成 canvas，进入到 render 方法：</p><p><img src="/blog/_assets/html2canvas-ForeignObjectRenderer-render1.80a6f7af.png" alt="html2canvas-ForeignObjectRenderer-render1"></p><p>render 方法执行很简单，首先通过 createForeignObjectSVG 将 DOM 内容包装到<code>&lt;foreignObject&gt;</code>中生成 svg:</p><p><img src="/blog/_assets/html2canvas-foreignObject-createForeignObjectSVG.5d98b1f5.png" alt="html2canvas-foreignObject-createForeignObjectSVG"></p><p>生成的 svg 如下所示：</p><p><img src="/blog/_assets/html2canvas-foreignObject-svg.de99fe85.png" alt="html2canvas-foreignObject-svg"></p><p>接着通过。loadSerializedSVG 将上面的 SVG 序列化成 img 的 src（SVG 直接内联），调用<code>this.ctx.drawImage(img, ...);</code> 将图片绘制到 <code>this.canvas</code> 上，返回生成好的 canvas 即可。</p><div class="language-js"><pre><code>document<span class="token punctuation">.</span>body<span class="token punctuation">.</span><span class="token function">appendChild</span><span class="token punctuation">(</span>canvas<span class="token punctuation">)</span>\n</code></pre></div><p>接着点击下一步，直到回到最开始的断点处，将生成好的 canvas 挂在到 DOM 上，如下：</p><p><img src="/blog/_assets/html2canvas-foreignObject-canvas.5ba9d434.png" alt="html2canvas-foreignObject-canvas.png"></p><p>这就解决了？？？收工 🤝</p><p>NoNoNo，<strong>为什么使用纯 canvas 绘制就有问题呢？</strong> 作为 bug 终结者，问题必须找出来，干就完了 👊。</p><blockquote><p>使用 foreignObject 渲染还有其他问题，后面再说。</p></blockquote><h4 id="foreignobject-绘制-canvas-的流程"><a class="header-anchor" href="#foreignobject-绘制-canvas-的流程" aria-hidden="true">#</a> foreignObject 绘制 canvas 的流程</h4><p><img src="/blog/_assets/html2canvas-foreignObject-flow.7c17659c.png" alt="html2canvas-foreignObject-flow"></p><ol><li>首先克隆原始 DOM，并将所有样式都转为行内样式，避免修改到页面；</li><li>构建 ForeignObjectRenderer 渲染器实例，调用 renderer 方法；</li><li>将 DOM 内容通过 createForeignObjectSVG 转为 svg 元素；</li><li>通过 loadSerializedSVG 将上面的 svg 序列化成 img 的 src；</li><li>通过 drawImage 绘制图片到 canvas 上。</li></ol><h4 id="使用纯-canvas-绘制"><a class="header-anchor" href="#使用纯-canvas-绘制" aria-hidden="true">#</a> 使用纯 canvas 绘制</h4><p>要想使用 canvas 绘制，那么就需要将 DOM 树转换为 canvas 可以使用的数据类型，html2canvas 使用 parseTree 方法来实现转换，我们来看下它的执行过程。</p><p>直接在调用 parseTree 方法处打断点，进入到 parseTree 方法内：</p><p><img src="/blog/_assets/html2canvas-canvas-pasteTree.d329296d.png" alt="html2canvas-canvas-pasteTree"></p><p>parseTree 的作用是将克隆 DOM 转换为 ElementContainer 树。</p><p>首先将根节点转换为 ElementContainer 对象，接着再调用 parseNodeTree 遍历根节点下的每一个节点，转换为 ElementContainer 对象。</p><p><img src="/blog/_assets/html2canvas-canvas-pasteNodeTree.6227fee5.png" alt="html2canvas-canvas-pasteNodeTree.png"></p><p>ElementContainer 对象主要包含 DOM 元素的信息：</p><div class="language-ts"><pre><code><span class="token keyword">type</span> <span class="token class-name">TextContainer</span> <span class="token operator">=</span> <span class="token punctuation">{</span>\n  <span class="token comment">// 文本内容</span>\n  text<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>\n  <span class="token comment">// 位置和大小信息</span>\n  textBounds<span class="token operator">:</span> TextBounds<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n<span class="token punctuation">}</span>\n\n<span class="token keyword">export</span> <span class="token keyword">class</span> <span class="token class-name">ElementContainer</span> <span class="token punctuation">{</span>\n  <span class="token comment">// 样式数据</span>\n  <span class="token keyword">readonly</span> styles<span class="token operator">:</span> CSSParsedDeclaration<span class="token punctuation">;</span>\n  <span class="token comment">// 当前节点下的文本节点</span>\n  <span class="token keyword">readonly</span> textNodes<span class="token operator">:</span> TextContainer<span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n  <span class="token comment">//  除文本节点外的子元素</span>\n  <span class="token keyword">readonly</span> elements<span class="token operator">:</span> ElementContainer<span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n  <span class="token comment">// 位置大小信息（宽/高、横/纵坐标）</span>\n  bounds<span class="token operator">:</span> Bounds<span class="token punctuation">;</span>\n  <span class="token comment">// 标志位，用来决定如何渲染的标志</span>\n  flags <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>\n  <span class="token operator">...</span>\n<span class="token punctuation">}</span>\n</code></pre></div><p>ElementContainer 对象是一颗树状结构，层层递归，每个节点都包含以上字段，形成一颗 ElementContainer 树，如下： <img src="/blog/_assets/html2canvas-canvas-ElementContainer.6966aab7.png" alt="html2canvas-canvas-ElementContainer"></p><p>继续下一步 👇</p><p>通过 CanvasRenderer 创建一个渲染器 renderer，创建 <code>this.canvas</code>和<code>this.ctx</code>上下文对象与 ForeignObjectRenderer 类似</p><p>得到渲染器后，调用 render 方法将 parseTree 生成的 ElementContainer 树渲染成 canvas，在这里就与 ForeignObjectRenderer 的 render 方法产生差别了。</p><p>canvas 在绘制节点时需要先<strong>计算出整个目标节点里子节点渲染时所展现的不同层级</strong>，因为 Canvas 绘图需要根据样式计算哪些元素应该绘制在上层，哪些在下层。元素在浏览器中渲染时，根据 W3C 的标准，所有的节点层级布局，需要<strong>遵循层叠上下文和层叠顺序的标准</strong>。所以会先调用 parseStackingContexts 方法将 parseTree 生成的 ElementContainer 树转为层叠上下文。</p><h5 id="什么是层叠上下文"><a class="header-anchor" href="#什么是层叠上下文" aria-hidden="true">#</a> 什么是层叠上下文</h5><p>概念不懂就看 MDN：<a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_positioned_layout/Understanding_z-index/Stacking_context" target="_blank" rel="noopener noreferrer">层叠上下文</a></p><p>首先我们都知道 CSS 是流式布局，也就是在没有浮动（float）和定位（position）的影响下，是不会发生重叠的，从上到下、由外到内按照 DOM 树去布局。</p><p>而浮动和定位的元素会脱离文档流，形成一个层叠上下文，所以如果想正常渲染，就需要得到它们的层叠信息。</p><p>可以想象一下：在我们的视线与网页之间有一条看不见的 z 轴，层叠上下文就是一块块薄层，而这些薄层中有很多 DOM 元素，这些薄层根据层叠信息在这个 z 轴上排列，最终形成了我们看到的多彩的页面。</p><p>画个图好像更形象些：</p><blockquote><p>白色为正常元素，黄色为 float 元素，蓝色为 position 元素</p></blockquote><p><img src="/blog/_assets/stacking-contex.f16c73ce.png" alt="stacking-context"></p><p>更多详细资料请阅读：<a href="https://www.zhangxinxu.com/wordpress/2016/01/understand-css-stacking-context-order-z-index/" target="_blank" rel="noopener noreferrer">深入理解 CSS 中的层叠上下文和层叠顺序</a></p><p>ElementContainer 树中的每一个 ElementContainer 节点都会产生一个 ElementPaint 对象，最终生成层叠上下文的 StackingContext 如下：</p><p><img src="/blog/_assets/html2canvas-canvas-StackingContext.ba0ab836.png" alt="html2canvas-canvas-StackingContext"></p><p>数据结构如下：</p><div class="language-ts"><pre><code><span class="token comment">// ElementPaint 数据结构如下</span>\nElementPaint<span class="token operator">:</span> <span class="token punctuation">{</span>\n  <span class="token comment">// 当前元素的container</span>\n  container<span class="token operator">:</span> ElementContainer\n  <span class="token comment">// 当前元素的border信息</span>\n  curves<span class="token operator">:</span> BoundCurves\n<span class="token punctuation">}</span>\n\n<span class="token comment">// StackingContext 数据结构如下</span>\n<span class="token punctuation">{</span>\n  element<span class="token operator">:</span> ElementPaint<span class="token punctuation">;</span>\n  <span class="token comment">// z-index为负的元素行测会给你的层叠上下文</span>\n  negativeZIndex<span class="token operator">:</span> StackingContext<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n  <span class="token comment">// z-index为零或auto、transform或者opacity元素形成的层叠上下文</span>\n  zeroOrAutoZIndexOrTransformedOrOpacity<span class="token operator">:</span> StackingContext<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n  <span class="token comment">// 定位或z-index大于等于1的元素形成的层叠上下文</span>\n  positiveZIndex<span class="token operator">:</span> StackingContext<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n  <span class="token comment">// 非定位的浮动元素形成的层叠上下文</span>\n  nonPositionedFloats<span class="token operator">:</span> StackingContext<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n  <span class="token comment">// 内联的非定位元素形成的层叠上下文</span>\n  nonPositionedInlineLevel<span class="token operator">:</span> StackingContext<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n  <span class="token comment">// 内联元素</span>\n  inlineLevel<span class="token operator">:</span> ElementPaint<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n  <span class="token comment">// 非内联元素</span>\n  nonInlineLevel<span class="token operator">:</span> ElementPaint<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>\n<span class="token punctuation">}</span>\n</code></pre></div><p>渲染层叠内容时会根据 StackingContext 来决定渲染的顺序。</p><p>继续下一步，调用 renderStack 方法，renderStack 执行 renderStackContent 方法，我们直接进入 renderStackContent 内：</p><p><img src="/blog/_assets/html2canvas-canvas-renderStackContent.dca3632d.png" alt="html2canvas-canvas-renderStackContent"></p><p>canvas 绘制时遵循 w3c 规定的渲染规则 <a href="https://www.w3.org/TR/css-position-3/#painting-order" target="_blank" rel="noopener noreferrer">painting-order</a>，renderStackContent 是对此规则的一个代码实现，步骤如下：</p><p>此处的步骤 1-7 对应上图代码中的 1-7:</p><ol><li>渲染当前层叠上下文的元素的背景和边框；</li><li>渲染具有负 z-index 级别的子层叠上下文（最负的第一个）；</li><li>对于流式布局、非定位的子元素调用 renderNodeContent 和 renderNode 进行渲染：</li><li>渲染所有未定位的浮动子元素，对于其中每一个，将该元素视为创建了一个新的堆栈上下文；</li><li>渲染正常流式布局、内联元素、非定位的子元素；</li><li>渲染 z-index 为 0 或 auto，或者 transform、opacity 等属性的子元素；</li><li>渲染由 z-index 大于或等于 1 的子元素形成的层叠上下文，按 z-index 顺序（最小的在前）。</li></ol><p>可以看到遍历时会对形成层叠上下文的子元素递归调用 renderStack，最终达到对整个层叠上下文树进行递归的目的：</p><p>而对于未形成层叠上下文的子元素，就直接调用 renderNode 或 renderNodeContent 这两个方法，两者对比，<strong>renderNode 多了一层渲染节点的边框和背景色的方法</strong>（renderNode 函数内部调用 renderNodeBackgroundAndBorders 和 renderNodeContent 方法）。</p><p>renderNodeContent 用于渲染一个元素节点里面的内容，分为八种类型：纯文本、图片、canvas、svg、iframe、checkbox 和 radio、input、li 和 ol。</p><p>除了 iframe 的绘制比较特殊：重新生成渲染器实例，调用 render 方法重新绘制，其他的绘制都是调用 canvas 的一些 API 来实现，比如绘制文字主要用 fillText 方法、绘制图片、canvas、svg 都是调用 drawImage 方法进行绘制。</p><p><img src="/blog/_assets/html2canvas-canvas-renderNode.b939265f.png" alt="html2canvas-canvas-renderNode"></p><p><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/CanvasRenderingContext2D" target="_blank" rel="noopener noreferrer">所有可能用到的 API</a></p><p>最终绘制到 <code>this.canvas</code> 上返回，至此，html2canvas 的调试就结束了。 🎉🎉🎉</p><h4 id="纯-canvas-绘制流程"><a class="header-anchor" href="#纯-canvas-绘制流程" aria-hidden="true">#</a> 纯 canvas 绘制流程</h4><p><img src="/blog/_assets/html2canvas-canvas-flow.97631e48.png" alt="html2canvas-canvas-flow"></p><ol><li>首先克隆原始 DOM，避免修改到页面</li><li>使用 parseTree 递归遍历 html，生成 ElementContainer 树（与原始 DOM 层级结构类似）</li><li>构建 CanvasRenderer 渲染器实例，调用 renderer 方法</li><li>遍历上一步生成的 ElementContainer，根据层叠规则生成层叠上下文 StackingContext（与原始 DOM 层级结构区别较大）</li><li>遍历层叠上下文，递归地对层叠上下文各层中的节点和子层叠上下文进行解析并按顺序绘制在 Canvas 上，针对要绘制的每个节点，主要有以下两个过程：</li><li>创建画布，根据上一步生成的层叠对象递归渲染，最终绘制到画布 canvas 上</li></ol><h2 id="针对此-bug-分析"><a class="header-anchor" href="#针对此-bug-分析" aria-hidden="true">#</a> 针对此 Bug 分析</h2><p>ok，当调试了一遍 html2canvas 的流程之后，再回到我们的问题上，很显然就是 canvas 渲染的时候的问题，也就是 renderNodeContent 方法，那我们直接在这里打个断点进行调试（为了方便我只输入一行文字进行调试），只有当是文本节点时会进入到此断点，等到 mark 标签中对应的元素进入断点时，查看：</p><p><img src="/blog/_assets/html2canvas-renderNodeContent.b6427ac2.png" alt="html2canvas-renderNodeContent.png"></p><p>可以看到此时 width 和 height 已经是父节点的宽高，果真如此 😱。</p><h2 id="解决方案"><a class="header-anchor" href="#解决方案" aria-hidden="true">#</a> 解决方案</h2><p>既然已经知道了问题所在，那么我们开始解决问题，有以下两种解决方案可供参考：</p><h3 id="foreignobjectrendering"><a class="header-anchor" href="#foreignobjectrendering" aria-hidden="true">#</a> foreignObjectRendering</h3><p>在 html2canvas 配置中设置 <code>foreignObjectRendering</code> 为 <code>true</code>，此问题就可以解决吗？</p><p>然而现实并没有这么简单，这样又会出现引出新的问题：导出的图片内容会丢失</p><p>这是为什么呢？</p><p>通过 W3C 对<a href="https://svgwg.org/specs/integration/#static-image-document-mode" target="_blank" rel="noopener noreferrer">SVG 的介绍</a>可知：<strong>SVG 不允许连接外部的资源</strong>，比如 HTML 中图片链接、CSS link 方式的资源链接等，在 SVG 中都会有限制。</p><p>解决方法：需要将图片资源转为 base64，然后再去生成截图，foreighnObject 这种方法更适合截取内容为文字内容居多的场景。</p><h3 id="对包含背景色的内联标签截断处理"><a class="header-anchor" href="#对包含背景色的内联标签截断处理" aria-hidden="true">#</a> 对包含背景色的内联标签截断处理</h3><p>在对内联元素进行截断前，<strong>如何确定 p 标签中的 mark 标签有没有换行？</strong> 因为我们没必要对所有内联标签做处理。</p><p>如果 mark 标签的高度超过 p 标签的一半时，就说明已经换行了，然后将 <code>&lt;mark&gt;要求一&lt;/mark&gt;</code> 替换为 <code>&lt;mark&gt;要&lt;/mark&gt;&lt;mark&gt;求&lt;/mark&gt;&lt;mark&gt;一&lt;/mark&gt;</code> 即可，代码如下：</p><div class="language-ts"><pre><code><span class="token keyword">const</span> <span class="token function-variable function">handleMarkTag</span> <span class="token operator">=</span> <span class="token punctuation">(</span>ele<span class="token operator">:</span> HTMLElement<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>\n  <span class="token keyword">const</span> markElements <span class="token operator">=</span> ele<span class="token punctuation">.</span><span class="token function">querySelectorAll</span><span class="token punctuation">(</span><span class="token string">&#39;mark&#39;</span><span class="token punctuation">)</span>\n  <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">let</span> sel <span class="token keyword">of</span> markElements<span class="token punctuation">)</span> <span class="token punctuation">{</span>\n    <span class="token keyword">const</span> <span class="token punctuation">{</span> height <span class="token punctuation">}</span> <span class="token operator">=</span> sel<span class="token punctuation">.</span><span class="token function">getBoundingClientRect</span><span class="token punctuation">(</span><span class="token punctuation">)</span>\n    <span class="token keyword">let</span> parentElement <span class="token operator">=</span> sel<span class="token punctuation">.</span>parentElement\n    <span class="token keyword">while</span> <span class="token punctuation">(</span>parentElement<span class="token operator">?.</span>tagName <span class="token operator">!==</span> <span class="token string">&#39;P&#39;</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>\n      parentElement <span class="token operator">=</span> parentElement<span class="token operator">?.</span>parentElement<span class="token operator">!</span>\n    <span class="token punctuation">}</span>\n    <span class="token keyword">const</span> <span class="token punctuation">{</span> height<span class="token operator">:</span> parentHeight <span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token punctuation">(</span>\n      parentElement <span class="token keyword">as</span> <span class="token builtin">unknown</span> <span class="token keyword">as</span> HTMLElement\n    <span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getBoundingClientRect</span><span class="token punctuation">(</span><span class="token punctuation">)</span>\n    <span class="token comment">// mark的高度没有超过p标签的一半时 则没有换行</span>\n    <span class="token keyword">if</span> <span class="token punctuation">(</span>height <span class="token operator">&lt;</span> parentHeight <span class="token operator">/</span> <span class="token number">2</span><span class="token punctuation">)</span> <span class="token keyword">continue</span>\n    <span class="token comment">// 超过一半时说明换行了</span>\n    <span class="token keyword">const</span> innerText <span class="token operator">=</span> sel<span class="token punctuation">.</span>innerText\n    <span class="token keyword">const</span> outHtml <span class="token operator">=</span> sel<span class="token punctuation">.</span>outerHTML\n    <span class="token keyword">let</span> newHtml <span class="token operator">=</span> <span class="token string">&#39;&#39;</span>\n    innerText<span class="token punctuation">.</span><span class="token function">split</span><span class="token punctuation">(</span><span class="token string">&#39;&#39;</span><span class="token punctuation">)</span><span class="token operator">?.</span><span class="token function">forEach</span><span class="token punctuation">(</span><span class="token punctuation">(</span>text<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>\n      newHtml <span class="token operator">+=</span> outHtml<span class="token punctuation">.</span><span class="token function">replace</span><span class="token punctuation">(</span>innerText<span class="token punctuation">,</span> text<span class="token punctuation">)</span>\n    <span class="token punctuation">}</span><span class="token punctuation">)</span>\n    sel<span class="token punctuation">.</span>outerHTML <span class="token operator">=</span> newHtml\n  <span class="token punctuation">}</span>\n<span class="token punctuation">}</span>\n</code></pre></div><p>ok，再次尝试一下，完美解决，这下可以收工了。</p><h2 id="总结"><a class="header-anchor" href="#总结" aria-hidden="true">#</a> 总结</h2><p>通过对一个 bug 的分析，尝试调试了一遍 html2canvas 的代码，弄懂了浏览器截图的原理及 html2canvas 的核心流程，并从中学到了几点新知识：</p><ol><li>svg xmnls 作用以及渲染 HTML 内容的 foreignObject 标签；</li><li>CSS 层叠上下文的概念；；</li><li>使用 foreignObject 实现快速截图的方法；</li><li>canvas 绘图的一些方法。。。</li></ol><p>发现 canvas 真是一个有趣的东西，什么都能画，像我现在用于画图的工具<a href="https://excalidraw.com/" target="_blank" rel="noopener noreferrer">excalidraw</a>、图表库<a href="https://g6.antv.antgroup.com/" target="_blank" rel="noopener noreferrer">g6</a>、<a href="https://g2.antv.antgroup.com/" target="_blank" rel="noopener noreferrer">g2</a>、<a href="https://echarts.apache.org/zh/index.html" target="_blank" rel="noopener noreferrer">echarts</a>都是用的 canvas 搞的，看来得抽时间学习一下 canvas，不要等到“书到用时方恨少“。</p>',132)];e.render=function(s,t,e,o,c,l){return n(),a("div",null,p)};export{t as __pageData,e as default};
